# A simple PHP WebSocket server example.

To run, open a terminal and type:
php5 ./server.php

Then visit index.html in your browser (You may need to set up a webserver to serve it).

You'll need IE10+, FF7+ or Chrome 14+ to run this example.

wsOnOpen -> stuur id naar client + encryptiestatus
wsOnClose -> stuur naar andere clients dat user weg is
wsOnMessage -> 
	Broadcast: gebruikersnaam, id, pub key
	Broadcast: StartEncryption + pas encryptiestatus server aan
	Unicast: Stuur bericht naar client X en naar presentatie
